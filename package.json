{
  "name": "df-cerebe",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "DF Cerebe — internal dashboard. Bun + Hono + LangGraph.js backend, Vite + Svelte frontend, gated by Dark Factory. Native dev, optional container/k8s deploy.",
  "license": "UNLICENSED",
  "packageManager": "bun@1.1.0",
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "//init": "------- ONE-TIME: rename this template to your product (Copier replacement). -------",
    "init": "bash scripts/init.sh",
    "//dev": "------- NATIVE DEV (no Docker, no k8s). This is the daily loop. -------",
    "dev": "bun run --filter '*' dev",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "//gate": "------- DARK FACTORY GATE (runs via Husky hooks; manual handles below). -------",
    "review": "df review --commit HEAD",
    "doctor": "df doctor",
    "prepare": "husky",
    "//deploy": "------- DEPLOY-ONLY (container/k8s). Never needed for dev. See deploy/README.md -------",
    "docker:build:backend": "docker build -f deploy/docker/backend.Dockerfile -t df-cerebe-backend:local .",
    "docker:build:frontend": "docker build -f deploy/docker/frontend.Dockerfile -t df-cerebe-frontend:local .",
    "k8s:apply": "kubectl apply -k deploy/k8s/base"
  },
  "devDependencies": {
    "@momentiq/dark-factory-cli": "2.5.0",
    "bun-types": "1.3.14",
    "husky": "9.1.7"
  }
}
