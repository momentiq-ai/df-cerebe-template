# Pin the @momentiq scope to the public npm registry.
# Defensive: npm's default is already this, but the Dark Factory consumer
# contract (CONSUMER-ADOPTION.md §4b) asks consumers to make it explicit so
# a hostile registry override can't redirect the gate CLI install.
@momentiq:registry=https://registry.npmjs.org/
